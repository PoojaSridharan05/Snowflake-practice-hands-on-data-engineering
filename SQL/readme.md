| File Name                           | Description                                                                                      |
| ----------------------------------- | ------------------------------------------------------------------------------------------------ |
| `Caching.sql`                       | Demonstrates Snowflake query caching mechanisms and performance optimization using result cache. |
| `Cloning-DB-Schemas.sql`            | Shows how to clone databases and schemas using Snowflake zero-copy cloning.                      |
| `Cloning-Table.sql`                 | Contains examples for cloning tables without duplicating physical storage.                       |
| `Cloning-with-TimeTravel.sql`       | Demonstrates cloning objects using Snowflake Time Travel feature.                                |
| `ClusterKey.sql`                    | Explains creation and usage of clustering keys for query performance optimization.               |
| `CopyData.sql`                      | Shows how to load and copy data into Snowflake tables using COPY command.                        |
| `Creating-Reader-Account.sql`       | Demonstrates how to create and manage Snowflake Reader Accounts for secure data sharing.         |
| `CreationOfWH.sql`                  | Contains commands for creating and managing Snowflake virtual warehouses.                        |
| `Data-Sampling.sql`                 | Demonstrates random and system data sampling techniques in Snowflake.                            |
| `Data-Sharing.sql`                  | Shows secure data sharing concepts and implementation in Snowflake.                              |
| `Fail-Safe-Storage.sql`             | Explains Snowflake Fail-safe storage and recovery concepts.                                      |
| `FileFormat.sql`                    | Demonstrates creation and usage of file formats such as CSV, JSON, and Parquet.                  |
| `FirstWork.sql`                     | Basic introductory Snowflake SQL operations and initial setup commands.                          |
| `HandleUnstructuredData.sql`        | Shows handling and querying semi-structured/unstructured data like JSON and XML.                 |
| `LoadHistory.sql`                   | Demonstrates how to check data load history and monitor ingestion processes.                     |
| `OnError.sql`                       | Contains examples of handling errors during data loading operations.                             |
| `ParquetData.sql`                   | Demonstrates loading and querying Parquet file data in Snowflake.                                |
| `Permanent-Table.sql`               | Explains creation and behavior of permanent tables in Snowflake.                                 |
| `Restoring-data-using-TT.sql`       | Shows restoring dropped or modified data using Time Travel.                                      |
| `Retention-Time.sql`                | Demonstrates configuring data retention periods for Time Travel.                                 |
| `Scheduling-Cron-Task.sql`          | Contains examples for scheduling Snowflake tasks using CRON expressions.                         |
| `Scheduling-tasks.sql`              | Demonstrates task scheduling and automation in Snowflake.                                        |
| `Secure-View.sql`                   | Shows how to create secure views for controlled data access.                                     |
| `Sharing-DB-Schema.sql`             | Demonstrates sharing database schemas securely between accounts.                                 |
| `Sharing-Data-From-Multiple-DB.sql` | Shows techniques for sharing data across multiple databases.                                     |
| `Sharing-secure-view.sql`           | Demonstrates sharing data securely using secure views.                                           |
| `SizeLimit.sql`                     | Explains file size limits and handling large data loads in Snowflake.                            |
| `SnowpipeCreation.sql`              | Demonstrates creating and configuring Snowpipe for continuous data ingestion.                    |
| `Stage.sql`                         | Contains examples for internal and external stages in Snowflake.                                 |
| `StorageIntegration.sql`            | Shows how to configure storage integrations with cloud providers like AWS S3, Azure, or GCP.     |
| `Stored-Procedure_task.sql`         | Demonstrates integrating stored procedures with Snowflake tasks.                                 |
| `Stream-Insert.sql`                 | Shows usage of streams for tracking inserted records.                                            |
| `Stream-Update.sql`                 | Demonstrates tracking updates and changes using streams.                                         |
| `Swapping-Tables-using-Clone.sql`   | Contains examples of swapping tables using clone operations for deployments or recovery.         |
| `Task-History-&-Error.sql`          | Demonstrates monitoring task execution history and troubleshooting task errors.                  |
| `Temporary-Table.sql`               | Explains creation and behavior of temporary tables.                                              |
| `Time-Travel-cost.sql`              | Shows storage cost implications of Snowflake Time Travel.                                        |
| `TimeTravel.sql`                    | Demonstrates Snowflake Time Travel functionality and querying historical data.                   |
| `TransformingData.sql`              | Contains SQL transformations and data manipulation examples.                                     |
| `Transient-Table.sql`               | Explains transient tables and their storage/cost benefits.                                       |
| `Tree-of-tasks.sql`                 | Demonstrates dependency-based task trees and workflow orchestration.                             |
| `TruncateColumn.sql`                | Shows examples related to truncating or modifying column data.                                   |
| `Undrop-table.sql`                  | Demonstrates recovering dropped tables using UNDROP command.                                     |
| `Untitled 3.sql`                    | Miscellaneous or experimental SQL queries and test scripts.                                      |
| `ValidationMode.sql`                | Demonstrates validation modes during data loading for error checking.                            |
| `readme.md`                         | Documentation and overview of the Snowflake hands-on SQL practice repository.                    |

